/target
# Cargo.lock SÍ se versiona: el workspace produce binarios (`crucible`,
# `instrusim`), y ahí el lock reproducible es lo correcto.
*.log
**/*.rs.bk
*.pdb

# Entornos de prueba Python (suite pyvisa)
.venv/
__pycache__/
.pytest_cache/

# Editores
.vscode/
.idea/
*.swp
